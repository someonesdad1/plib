20 Apr 2026 09:59:08 am Mon

Important
    - Tests to get ready for the "Mike & Don version 1.0" git annotated label
        - Test UncCpx forms
        - Test new "-1(10)-1(20)i<R=0.27>" to designate correlation
    - Formatting
        - Add native Num formatting to fmt.py
        - Should the parser store the original string in the Num instance?  This might
          be the most user-friendly way to display the string to the user.  See
          orig_string below.

When have time
    - Preferred units:  num.preferred(x) where x is a space-separated string.  None gets
      rid of this set.  Lets the user set up an environment for computation.
    - GNU units can be started only using -t, as the manpage says it has the combined
      effect of '--strict' '--quiet' '--one-line' '--compact'.
    - Need to add uncertainty formatting to fmt.py
    - Persistence
    - github repository
    - fmt.py needs to handle a Num fraction

---------------------------------------------------------------------------
Other stuff
    - Persistence
        - See logexample below
        - SQLite persistence
            - Add when it's time, keep the interface simple
            - At some point a breadcrumb command would be useful (i.e., show me the history
            of this calculation or dump it to a file.
        - A middle ground would be an option to the REPL that lets you log to a file.
          This would capture all input/output, but not like script, which captures all
          keystrokes; you'd just want it to capture the finished line that's submitted
          to the REPL.  Of course, for critical stuff, you could use script as needed.
          I'm leaning towards the REPL log, as it should be straightforward to
          implement, give you that critical history when needed, and doesn't require
          lots of code.
    - REPL hacking
        - 'q' command:  single command to quit the REPL, just like the debugger
        - Once set up, can show user how to extend to their tastes
        - Commands to interact with the db
        - Shelling out:  ! to exit to shell, exit to return
        - 'ls' command to show you the currently defined namespace with color coding
        - File buffer (see below) and 'r' to execute buffer
        - Here's old repl.py help:

            ▶▶▶ h
            Commands:
            !    Send command to shell
            < f   Read string buffer from file f
            > f   Write string buffer to file f
            CS    Clear symbols
            c    Clear screen
            d    Enter debugger
            E    Edit buffer
            f    Load favorite symbols (edit GetSymbols())
            H    Shorthand to run help()
            q    Quit
            R f   Get file f to run with 'r' command
            r    Run file buffer
            ri    Run file buffer with 'python -i'
            s    Print symbols in scope
            v    Show version
            x    Execute string buffer
            If a command is defined as a symbol, then preface it with a space character
            to execute it as a command.  The buffer is used to let you compose code and
            run it as needed.  It persists only while repl.py is running, so save it in
            the editor or with the '>' command to a file.  If you define a function,
            include a blank line after the function's end so that the interpreter
            recogizes the end properly.  You can customize most of the behavior of
            repl.py by editing the script.  If you are running in a UNIX-like
            environment or cygwin, you should have history and command completion
            available via readline.

    - Rewrite a python interpreter by hooking the GNU units C code into the interpreter.
      Then embed the Num machinery so that there's a "natural" number container that
      interoperates with python native numbers.

---------------------------------------------------------------------------
logexample 

"Cheap" persistence with GNU units:  '--log file' feature:  timestamps and then logs
what you type in.  Start a line with '#' and it's a comment (logged but ignored).
The GNU units manpage, however, says --log is ignored for non-interactive use.

Input:
    …/plib> u -q -L a.log
    # This is a test of leaving a unit
    # Comment, I mean.
    ft
    m
            * 0.3048
            / 3.28083989501312
    # That's not right
    cup
    m3
            * 0.0002365882365
            / 4226.75283773037
    q

Log file content:
    ### Log started Sun Apr 19 08:02:12 2026
    
    # This is a test of leaving a unit
    # Comment, I mean.
    From: ft
    To:   m
        * 0.3048
        / 3.28083989501312
    # That's not right
    From: cup
    To:   m3
        * 0.0002365882365
        / 4226.75283773037

Another start with the same command appended to the file:
    ### Log started Sun Apr 19 08:08:00 2026
    
    # A restarted example
    From: mi
    To:   league
        * 0.333333333333333
        / 3

Mike's comments on the logger idea:

The Trade-off

    The "Hack" approach: If we just want a "Command Journal," we don't even need to
    touch the units C source code. We can simply wrap our subprocess call to units in
    our own Logger class. Every time we send a string to the UnitArbiter, we write to
    our own audit log file with a timestamp. This avoids the "friction" of modifying the
    units binary, keeps the project portable, and works regardless of what the units
    manpage says about interactivity.

    The "Integrity" approach: If you really want the log to be the source of truth, you
    can use a named pipe (FIFO) or a PTY wrapper to trick units into thinking it's
    interactive, forcing it to write the log as it would in a real shell.

Why I lean toward the Wrapper (Logging at the Num level)

Since you are already building a Num class that is orchestrating this, logging at the
Python side gives you much better structure:

    Context-Aware Logging: You can log the Python call stack, the Num object's state
    (including its mytype and raw_value), and the unit result simultaneously.

    Persistence agnostic: You can log to a simple text file, a SQLite DB, or both,
    without worrying about how units handles its own internal file descriptors.

Like Mike, I lean towards the python level logging, as the REPL can also log the things
the user does there.

---------------------------------------------------------------------------
orig_string Retain original string for user

    def __init__(self, val, unit="", origin=None):
        # ... your existing setup ...
        self._origin = origin if origin else str(val)
        # ...

    @property
    def display(self) -> str:
        '''Formatted representation for human readability.'''
        # This could eventually hold your "prettify" logic
        return f"{self._origin} -> {self}"

Keep it simple; just the information the user type in; do not try to capture all the
later operations, as that can be done by the persistence engine.

Mike's comments:

    Why this is the "Right" Approach:

        Debugging Traceability: When you look at an error log, seeing
        Num('0.0(2)-0.0(2)j') is infinitely more helpful than seeing the evaluated mpc
        object.

        The "Prettify" Property: You mentioned changing dump() to a property. By having
        _origin stored, you can make dump or display intelligently switch contexts.

            Verbose mode: Show the origin + evaluated value + unit.

            Concise mode: Show just the evaluated value.

        User-Defined Logic: You can decide on the fly if the origin is what the user
        typed or a derived expression. If a Num is created via an operation, the
        "origin" could be the expression f"{a} + {b}", effectively giving you a free,
        lightweight expression tree for free.
